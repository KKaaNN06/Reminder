{% extends 'base.html.twig' %}
{% block title %}Klassen{% endblock %}
{% block body %}
<h1>Klassen</h1>
<div class="list-group">
    {% for group in groups %}
        <a class="list-group-item list-group-item-action" href="{{ path('group_show', {'id': group.id}) }}">
            {{ group.name }}
        </a>
    {% else %}
        <div class="list-group-item">Nog geen klassen</div>
    {% endfor %}
</div>
{% endblock %}
